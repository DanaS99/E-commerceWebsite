import React from 'react'
import { Pie, Line } from 'react-chartjs-2';
import 'chart.js/auto'

const AdminStatsChart = ({stats}) => {
  const pie = {
    labels: ['Active', 'Inactive'],
    datasets: [
        
    ]
  }
  
    return (
    <div>AdminStatsChart</div>
  )
}

export default AdminStatsChart